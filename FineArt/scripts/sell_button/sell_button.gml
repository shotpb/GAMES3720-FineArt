// I don't understand scripts on a fundamental level so this code sucks really bad.

function sell_button(x, y) constructor{
	sell_button_instance = instance_create_layer(x, y, "Instances", o_button);
}